import React from 'react';
import './Title.css';

const Title = props => (
    <div className = "titlebox">
        <h1> Remember the Dog! </h1>
        <p> Click on an image to earn points, but remember the dogs! You don't want to click on them more than once! </p>
    </div>
);

export default Title;